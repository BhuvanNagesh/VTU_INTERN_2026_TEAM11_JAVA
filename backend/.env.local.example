# ─────────────────────────────────────────────────────────────────────────────
# LOCAL DEVELOPMENT ENVIRONMENT VARIABLES
# Copy this file to .env.local and fill in your values.
# Never commit .env.local — it is in .gitignore.
# ─────────────────────────────────────────────────────────────────────────────

# To use these in your local Spring Boot run, either:
#  1) Set them as system environment variables before running the app, OR
#  2) In IntelliJ: Run > Edit Configurations > Environment Variables

DB_URL=jdbc:postgresql://db.pkktouewaqhvnwahoktn.supabase.co:5432/postgres?sslmode=require
DB_USERNAME=postgres
DB_PASSWORD=wealthwise@2004

JWT_SECRET=WealthWise$ecretKey2026!SuperSecureRandomString#MF

MAIL_USERNAME=bhuvannagesh2004@gmail.com
MAIL_PASSWORD=ksyn srtg uaix slza

CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:4173
